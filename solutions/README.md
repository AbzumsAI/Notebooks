# Solution Notes

I keep these files as checked reference answers for the starter notebooks. The Python files are easier to test, and the notebooks are easier to read beside the homework files.

Run this check from the repo root:

```powershell
python scripts/check_solution_files.py
```

The Mastermind answer uses a fake request function in tests, so the check does not need the live API.
