# Privacy check

Notebooks can accidentally keep private data in code cells, markdown cells, outputs, or hidden metadata.

Before sharing a solved copy, search for tokens, passwords, private links, and personal data that the course did not request.

Replace sample contact data with placeholders when it is only used as an example.
