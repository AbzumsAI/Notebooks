# Package notes

Most assignments can run with common data tools such as NumPy, pandas, and matplotlib.

Install packages in the same environment that runs the notebook kernel.

When an import fails, check the kernel name shown by the notebook before installing more packages.
