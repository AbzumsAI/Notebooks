# Local setup

I usually run these notebooks with JupyterLab or VS Code.

A simple setup is:

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install notebook jupyterlab numpy pandas matplotlib
```

Use the same environment when installing any package needed by an assignment.
