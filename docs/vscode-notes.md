# VS Code notes

VS Code works well when the Python extension and Jupyter support are installed.

Open the repo folder, pick the right Python environment, then run the notebook cells in order.

If a package import fails, install it in the selected environment and rerun the first cells.
