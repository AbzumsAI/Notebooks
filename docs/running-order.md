# Running order

Run notebooks from the first cell to the last cell.

Skipping earlier cells can leave variables undefined or stale. After a big edit, restart the kernel and run all cells again.

Before submission, I check that the saved outputs match the latest code.
